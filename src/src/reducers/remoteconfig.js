import {
 
} from '../actions/remoteconfig';

const initialState = {

};

export default function remoteconfig(state = initialState, action = {}) {
  switch (action.type) {
   
    default:
      return state;
  }
}
